import { BIGA, VALIDATOR, WINDOW_DURATION, WITHDRAWAL_LIMIT } from './config.json';
import { NAME, SUPPLY, SYMBOL } from './token.json';
import { BIGA_STAKING, STAKING_TOKEN, STAKING_OWNER } from './staking.json';
export const BigaConfigs = { BIGA, VALIDATOR, WITHDRAWAL_LIMIT, WINDOW_DURATION };
export const TokenConfigs = { NAME, SYMBOL, SUPPLY };
export const BigaStakingConfigs = { BIGA_STAKING, STAKING_TOKEN, STAKING_OWNER };
